#include "stdafx.h"
#include "IntersectionGenerator.h"
#include "algorithm/grap_point_algorithm.h"
#include <algorithm>
namespace OMDB
{
    void IntersectionGenerator::generate(DbMesh* const pMesh)
    {
		UNREFERENCED_PARAMETER(pMesh);
    }

    void IntersectionGenerator::generateRelation(DbMesh* const pMesh, std::vector<DbMesh*>* nearby)
    {
		UNREFERENCED_PARAMETER(pMesh);
		UNREFERENCED_PARAMETER(nearby);
    }
}
